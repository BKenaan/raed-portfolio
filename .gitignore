# OS / editor
.DS_Store
Thumbs.db
*.log

# Dependencies (none required, but just in case)
node_modules/

# Local env
.env
.env.local

# Business-card build artifacts (fonts, generator scratch); keep the final PNG/PDF/spec
business-card/.build/
